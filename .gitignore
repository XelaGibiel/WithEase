__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/
.env

# IDE
.vscode/
.idea/
*.iml

# Tests
.pytest_cache/
.coverage
htmlcov/

# OS
Thumbs.db
.DS_Store

# WithEase user data (never commit personal settings).
# Real user data lives in %APPDATA%/WithEase/ (outside the repo) and can
# never be committed. These patterns additionally guard against accidental
# local runtime data if the app is ever run from inside the project folder.
/app.json
*.log

# Local chat logs / planning notes
Chatverl*/

# Local virtual environment and dev scripts
.venv/
check.ps1

# PyInstaller (auto-generiert; Build via BUILD_EXE.bat)
*.spec
